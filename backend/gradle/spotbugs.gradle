spotbugs {
    toolVersion = '4.5.1'
    ignoreFailures = false
    showProgress = true
    effort = 'max'
}

spotbugsMain {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}

spotbugsTest {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}
