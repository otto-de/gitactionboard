apply plugin: 'pmd'

pmd {
    ruleSets = []
    ruleSetFiles = files("${projectDir}/gradle/config/pmd.xml")
    toolVersion = '6.41.0'
    consoleOutput = false
}

tasks.withType(Pmd).configureEach {
    reports {
        xml.required = false
        html.required = true
    }
}
