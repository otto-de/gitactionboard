logging.config=
server.url=http://localhost:${mockServerPort}
wiremock.webhook.url=/webhook/notifications
wiremock.workflow.url=/workflow/notifications
REPO_OWNER_NAME=johndoe
REPO_NAMES=hello-world
DOMAIN_NAME=${server.url}
MS_TEAMS_NOTIFICATIONS_WEB_HOOK_URL=${server.url}${wiremock.webhook.url}
MS_TEAMS_NOTIFICATIONS_WORKFLOW_URL=${server.url}${wiremock.workflow.url}
spring.cache.type=none
ENABLE_GITHUB_SECRETS_SCAN_ALERTS_MONITORING=true
ENABLE_GITHUB_CODE_SCAN_ALERTS_MONITORING=true
logging.level.org.mockserver.log.MockServerEventLog = off
sqlite.db.file.path=gitactionboard_test.db
spring.test.database.replace=none
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-main.xml
