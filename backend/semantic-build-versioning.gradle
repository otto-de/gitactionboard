startingVersion = '1.0.0'
tagPrefix = 'v'
tagPattern = ~/^v/

autobump {
    majorPattern = ~/(?m)^.*!\s*:.*/
    minorPattern = ~/(?m)^feat\s*:.*/
    patchPattern = ~/(?m)^(fix|perf)\s*:.*/
    newPreReleasePattern = null
    promoteToReleasePattern = null
}
